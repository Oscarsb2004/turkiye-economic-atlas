# registry/datasets/cosmos-galaxies.yaml - one dataset the pipeline makes.
# atlas/core/registry.py checks every claim below.

title: "Every 2MASS Redshift Survey galaxy receding from us, as HEASARC serves it"
group: cosmos
order: 30
builder: atlas.datasets.cosmos:build_galaxies
params: {}
sources:
- nasa_heasarc
shells:
- fetcher
after: []
outputs:
- data/cosmos/galaxies.json
frame: null
on_screen: true
consumed_by:
- path: web/src/data/bundle.ts
  how: the cosmos view draws each galaxy at velocity / H0 megaparsecs in the direction the catalogue gives
