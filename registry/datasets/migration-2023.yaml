# registry/datasets/migration-2023.yaml - one year of the migration matrix.
# atlas/core/registry.py checks every claim below.

title: "Province-to-province migration in 2023, as TÜİK publishes it"
group: migration
order: 2023
builder: atlas.datasets.migration_flows:build
params:
  year: 2023
sources:
- tuik_internal_migration
shells:
- fetcher
after: []
outputs:
- data/migration/2023.json
frame:
  profile: panel
  record_type: observation
on_screen: true
consumed_by:
- path: web/src/data/bundle.ts
  how: the app fetches 2023.json, shades the map by a province's totals and draws its flows as arcs
