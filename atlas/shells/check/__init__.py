"""
Check shells: operations that pass or name what is wrong, inside the pipeline.

These are the pipeline's own guards. `verify/` re-checks the published outputs
independently and imports none of this (CLAUDE.md §4).
"""
